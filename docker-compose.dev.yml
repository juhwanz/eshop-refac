services:
  redis:
    image: redis:latest
    ports:
      - "127.0.0.1:${REDIS_PORT:-6380}:6379"
    environment:
      TZ: Asia/Seoul
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
